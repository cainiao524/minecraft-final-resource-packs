# 项目协作规则

## 架构
保持 `minecraft`、`mods`、`analysis`、`style`、`generation`、`image`、`validation`、`state` 与 `pipeline` 分层，不在扫描器中调用 AI。

## 生成文件
`project_state/` 中的状态文件由程序维护，不要手工修改。

## 测试
解析器、Diff、State 和图像校验的修改必须补充测试；测试使用自建最小 ZIP/PNG 固件，不提交版权资源。

## 用户资产
禁止删除或覆盖用户源材质。检测到用户手改时默认保留并报告冲突。

## 图像生成
AI raw 输出必须经过处理和 validation 后才能进入最终材质包。

## 向后兼容
State schema 变更需要保留读取旧版本 JSON 的能力。

## 范围
每次只做小步、可验证的修改。

## 面向未来版本的材质包适配流程

- 开始适配前记录目标 Minecraft 版本、对应官方 JAR 的 SHA-1、资源格式版本和基准目录；保留原始材质包、用户手改文件及冲突清单。
- 先扫描命名空间、路径和资源清单，再处理模型、方块状态、物品、纹理、图集、字体和界面；扫描器只做确定性检查，不调用 AI。
- 对每个界面 PNG 同时核对尺寸、透明度几何和九宫格元数据；重点检查创造模式标签、搜索栏、容器槽位、按钮、图标、精灵和全景背景。动画 PNG 必须保留匹配的 `.mcmeta` 帧数、尺寸和时间。
- 校验模型父链、纹理变量、装备模型、图集、字体和特殊渲染器引用；对目标版本新增或删除的资源生成待人工确认清单，禁止静默丢弃未知资源。
- 所有 PNG 必须真实解码并检查尺寸、透明度、动画帧和颜色细节；AI raw 输出先经过处理与 validation，再进入最终包。源包有意的纯色或透明效果要记录为设计决策，不能误判成缺陷。
- 生成包前运行完整离线结构审计和项目测试，确认 ZIP 路径、CRC、JSON 重复键、模型引用、GUI 几何和动画均无错误；输出包附带 SHA-256 和复查报告。
- 更新版本时保持旧版 `project_state` JSON 的读取兼容，不手工修改状态文件；最终输出写入 `workspace/output/`，不得覆盖用户源材质。
